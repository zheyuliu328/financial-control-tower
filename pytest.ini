[pytest]
testpaths = tests
pythonpath = src .
addopts = -ra
markers =
    unit: isolated unit/contract tests
    e2e: installed CLI and failure-path tests
    integration: optional external-service tests (none required by the offline demo)
